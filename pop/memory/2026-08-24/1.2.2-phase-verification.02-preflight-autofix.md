---
task: 1.2.2-phase-verification
entry: 02-preflight-autofix
---

# Preflight auto-executável com `-b main`

Comando explícito do dono: o preflight passou de "detecta e instrui" para "detecta e executa". Falhando o submódulo, o script roda `git submodule update --init modules/bastardkb`; falhando `user.qmk_home`, roda `qmk setup -b main bastardkb/bastardkb-qmk`. Sem confirmação; cada correção é revalidada e, se persistir, aborta com `exit 1` e o comando corretivo — fail-fast preservado. QMK CLI ausente segue só detecta-e-instrui; flash opt-in (`--flash`) e códigos 0–4 intactos.

## Evidence

- [[scripts/build_flash.sh]] — *auto-fix e revalidação, linhas 184–232*.
- [[pop/specs/build-flash-script|build-flash-script]] — *contrato do novo comportamento*.
