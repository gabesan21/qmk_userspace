---
task: 1.2.1-script-build-flash
entry: 02-spec-build-flash-script
---

# Spec draft `build-flash-script`

Contrato durável da CLI de build/flash, criado em 004 via write-spec: flags (`-k`/`-m`/`--flash`/`-h`), target padrão, checks de preflight, códigos de saída 0–4 e invariantes (flash nunca implícito; o script detecta e instrui, nunca configura; nenhum artefato de build dentro do repo). Status draft; questão aberta registrada: validar o nome do volume `RPI-RP2` e seus pontos de montagem na primeira execução real (task 1.2.2).

## Evidence

- [[pop/specs/build-flash-script|build-flash-script]] — *o spec criado nesta task*.
