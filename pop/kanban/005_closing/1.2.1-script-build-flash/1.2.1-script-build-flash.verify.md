# Judgment — [[1.2.1-script-build-flash]]

- **Stage:** 005_closing (act 1) · **Owner:** Judge Dredd (single judge)

## Round 1 — 2026-08-24

- **Strategy:** full — `critical: true` (005 full por regra; round 1, sem retornos).
- **Surface:** diff `main...ef2a361` no worktree da task — `scripts/build_flash.sh` (178 linhas, modo 755) e `pop/specs/build-flash-script.md` (72 linhas, novo draft autorizado no plano).
- **Returns:** execution 0 of 2 · plan 0 of 2.

### Pedido original primeiro (What/Why)

O card pede `scripts/build_flash.sh` executável, visualmente claro no CLI, com instruções e passos bem definidos: checks (qmk instalado, submódulo `modules/bastardkb`, `user.qmk_home`), compile do padrão `bastardkb/dilemma/3x5_3_procyon` + keymap `vendor` e flash via UF2 — com a execução reservada ao dono (comando de 2026-08-24). **Atendido:** os 3 checks existem, fail-fast, cada um com o comando corretivo exato (L88–109); o default é o pedido e é sobrescrevível por `-k`/`-m` (L17–18, L66–78); o flash é estritamente opt-in via `--flash` (L136–140); a saída tem passos numerados 1/3–3/3 com símbolos ✔/✘/➜ em pt-BR e cores respeitando `NO_COLOR` (L30–42); e o agente nunca executou o script — C7 nasceu `verify: user` e segue ao checklist humano.

### Objective and spec conformance

| # | Criterion | Mode | Verification | Result | Evidence |
|---|---|---|---|---|---|
| C1 | Script existe, +x, bash válido | evidence | `bash -n` + `shellcheck` (registrados) + leitura | passed | shebang + `set -euo pipefail` (L1, L12); lint 0 avisos; modo 755 confirmado |
| C2 | 3 checks de preflight com correção exata | evidence | leitura | passed | qmk L88–92; submódulo L95–100; `user.qmk_home` L103–108; todos `exit 1` com `Corrija com:` |
| C3 | Compile `qmk compile`, default + override | evidence | leitura | passed | L17–18, L118; override `-k`/`-m` L66–78; padrões documentados no `--help` L50–54 |
| C4 | Flash só com `--flash`, instruções de bootloader | evidence | leitura | passed | sem flag: L136–140 encerra sem gravar e exibe o comando; instruções L142–149 |
| C5 | CLI clara: uso/`--help`, passos visíveis, pt-BR | evidence | leitura | passed | `uso()` L44–61 com exemplos; `Passo 1/3…3/3` L86/114/134 |
| C6 | Diff só em `scripts/` + spec; sem artefatos | evidence | `pop_check_scope.py` + diff | passed | "valid ownership: 2 changed path(s)"; nenhum `*.uf2`/`*.hex`/`*.bin` no diff |
| C7 | Execução ponta-a-ponta pelo dono | human | checklist da entrega | recorded (human) | não julgado aqui — vai ao checklist humano do veredito |

### Consistência com o spec

`pop/specs/build-flash-script.md` confere com o script ponto a ponto: flags e padrões (Interfaces ↔ L17–18, L66–78), códigos de saída 0/1/2/3/4 (↔ L75, L91/99/107, L69/72/76, L120/127, L169/176), invariantes (flash nunca implícito; detecta-e-instrui sem configurar; `NO_COLOR`; compile escreve em `user.qmk_home`, não no repo) e erros/limits com os mesmos comandos corretivos. A questão aberta do volume `RPI-RP2` já está registrada no spec para a 1.2.2 — dívida já rastreada, não finding.

### Implementation quality

Leitura via clean-code-review (comportamento, bordas, complexidade, nomes, erros, docs). Bordas auditadas: `encontra_volume_uf2` retorna 0 sem candidato (if sem condição verdadeira → 0), logo não dispara `set -e` na atribuição L162; `nullglob` fica no subshell da command substitution e não vaza; glob `RPI-RP2*` cobre os 3 pontos de montagem do spec; nome do `.uf2` segue a convenção do QMK (`bastardkb_dilemma_3x5_3_procyon_vendor.uf2`, L123); `qmk config` parseado com fallback para `None`/inexistente (L103–104). Nenhum finding passou o teste de materialidade.

| Severity | Finding | Evidence | Required correction |
|---|---|---|---|
| — | nenhum (blocking 0 · suggestion 0 · nit 0) | — | — |

### Scope and integration

- [x] Changes respect every front's `Owns` and `Must not edit` — apenas `scripts/build_flash.sh` e o spec draft autorizado; gate de importação da Epoch 1 intacto.
- [x] Consumers did not opportunistically implement dependencies.
- [x] `verify: phase` criteria recorded in the phase checklist — plano não tem critério `phase`; C7 é `user`.
- [x] Affected specs, docs and DOX contracts are coherent.

## Verdict

- **Decision:** approved → delivery and close-out.
- **Blocking findings:** none.
- **Plan defect:** none.
- **Suggestions/nits:** none.
- **Human checklist:** C7 — rodar `scripts/build_flash.sh` (esperado: preflight passa e o `.uf2` do target padrão é gerado) e depois `scripts/build_flash.sh --flash` com o Dilemma em bootloader UF2 (esperado: volume `RPI-RP2` detectado em até 60 s e flash concluído com reboot do teclado).
- **Summary:** pedido integralmente atendido — script executável com preflight instrutivo, compile do target padrão e flash UF2 opt-in, mais o spec draft do contrato; escopo limpo, zero execução pelo agente.

<!-- pop-verdict round=1 decision=aprovada -->
